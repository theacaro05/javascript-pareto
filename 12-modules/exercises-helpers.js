// This file is the helper your 3-exercises.js will import from.
// Fill in the exports below — your tests will check them.

// TODO 1: Replace `undefined` with 100.
export const MAX_USERS = undefined;

// TODO 2: Replace `undefined` with an arrow function: (n) => n * 2
export const double = undefined;

// TODO 3: Replace `undefined` with an arrow function: (n) => n * 3
export const triple = undefined;

// TODO 4: Replace the line below so the default export is a function
//         (name) => "Welcome aboard, " + name + "!"
const welcome = undefined;
export default welcome;
